apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hiri-heatmap-test
  namespace: hiri
spec:
  rules:
  - host: my-test-host.test.fedcloud.eu
    http:
      paths:
      - backend:
          serviceName: hiri-heatmap-test-ingress
          servicePort: 8080
        path: /
        pathType: ImplementationSpecific

